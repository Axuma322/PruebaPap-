<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Evaluación: Neuroliderazgo, Motivación y Autorregulación</title>
<style>
  :root{
    --primary:#1f4e5f;
    --accent:#e07a3f;
    --bg:#f6f3ee;
    --card:#ffffff;
    --text:#2b2b2b;
    --correct:#2e7d32;
    --incorrect:#c62828;
    --border:#ddd6c9;
  }
  *{box-sizing:border-box;}
  body{
    margin:0;
    font-family:'Georgia', 'Times New Roman', serif;
    background:var(--bg);
    color:var(--text);
    padding:24px 12px 60px;
  }
  .container{max-width:820px;margin:0 auto;}
  header{
    background:var(--primary);
    color:#fff;
    padding:28px 24px;
    border-radius:10px;
    margin-bottom:24px;
  }
  header h1{margin:0 0 6px;font-size:1.5rem;}
  header p{margin:0;font-size:0.95rem;opacity:0.9;}
  .badge{
    display:inline-block;
    background:var(--accent);
    color:#fff;
    padding:3px 10px;
    border-radius:20px;
    font-size:0.75rem;
    margin-top:10px;
    font-family:Arial, sans-serif;
    letter-spacing:0.5px;
  }
  .section-label{
    font-family:Arial, sans-serif;
    text-transform:uppercase;
    letter-spacing:1px;
    font-size:0.75rem;
    color:var(--accent);
    font-weight:bold;
    margin:30px 0 6px;
    border-bottom:2px solid var(--accent);
    padding-bottom:4px;
  }
  .question{
    background:var(--card);
    border:1px solid var(--border);
    border-radius:8px;
    padding:18px 20px;
    margin-bottom:14px;
  }
  .question p.qtext{
    font-weight:bold;
    margin:0 0 12px;
    font-size:1.02rem;
  }
  .qnum{color:var(--primary);margin-right:6px;}
  .options label{
    display:block;
    padding:8px 10px;
    border-radius:6px;
    margin-bottom:6px;
    cursor:pointer;
    font-family:Arial, sans-serif;
    font-size:0.95rem;
    transition:background 0.15s;
  }
  .options label:hover{background:#f0ece2;}
  .options input{margin-right:8px;}
  .question.correct{border-left:5px solid var(--correct);}
  .question.incorrect{border-left:5px solid var(--incorrect);}
  .feedback{
    display:none;
    margin-top:10px;
    padding:8px 10px;
    border-radius:6px;
    font-family:Arial, sans-serif;
    font-size:0.88rem;
  }
  .feedback.show{display:block;}
  .feedback.correct{background:#e8f5e9;color:var(--correct);}
  .feedback.incorrect{background:#fdecea;color:var(--incorrect);}
  #submitBtn{
    display:block;
    width:100%;
    padding:14px;
    background:var(--primary);
    color:#fff;
    border:none;
    border-radius:8px;
    font-family:Arial, sans-serif;
    font-size:1.05rem;
    font-weight:bold;
    cursor:pointer;
    margin-top:24px;
  }
  #submitBtn:hover{background:#173d4a;}
  #resultBox{
    display:none;
    margin-top:24px;
    background:var(--card);
    border:2px solid var(--primary);
    border-radius:10px;
    padding:24px;
    text-align:center;
    font-family:Arial, sans-serif;
  }
  #resultBox.show{display:block;}
  #resultBox h2{color:var(--primary);margin-top:0;}
  .scoreNum{font-size:2.6rem;font-weight:bold;color:var(--accent);}
  .breakdown{
    display:flex;
    justify-content:center;
    gap:30px;
    margin-top:16px;
    flex-wrap:wrap;
  }
  .breakdown div{font-size:0.9rem;}
  .breakdown strong{display:block;font-size:1.3rem;color:var(--primary);}
  .missing-warning{
    color:var(--incorrect);
    font-family:Arial, sans-serif;
    font-size:0.9rem;
    text-align:center;
    margin-top:10px;
    display:none;
  }
  .missing-warning.show{display:block;}
</style>
</head>
<body>
<div class="container">
  <header>
    <h1>Evaluación de Submódulo</h1>
    <p>Neuroliderazgo, Motivación y Autorregulación</p>
    <span class="badge">10 preguntas · Selección única</span>
  </header>

  <form id="quizForm">
    <div class="section-label">Bloque I · Neuroliderazgo (5 preguntas)</div>
    <div id="liderazgoBlock"></div>

    <div class="section-label">Bloque II · Motivación y Autorregulación (5 preguntas)</div>
    <div id="motivacionBlock"></div>

    <p class="missing-warning" id="missingWarning">Por favor responde todas las preguntas antes de enviar.</p>
    <button type="button" id="submitBtn">Enviar y calcular resultado</button>
  </form>

  <div id="resultBox">
    <h2>Resultado de la evaluación</h2>
    <div class="scoreNum" id="scoreNum">0 / 10</div>
    <p id="scorePercent"></p>
    <div class="breakdown">
      <div><strong id="liderazgoScore">0/5</strong>Neuroliderazgo</div>
      <div><strong id="motivacionScore">0/5</strong>Motivación y Autorregulación</div>
    </div>
    <p id="scoreMessage" style="margin-top:16px;font-style:italic;"></p>
  </div>
</div>

<script>
const liderazgoQuestions = [
  {
    q:"El neuroliderazgo se define como un enfoque que aplica los hallazgos de la neurociencia para:",
    options:["Sustituir por completo la experiencia y el criterio del líder","Comprender y mejorar la manera en que las personas piensan, deciden y se relacionan en contextos de liderazgo","Eliminar la necesidad de comunicación entre líder y equipo","Centrarse únicamente en indicadores financieros de la organización"],
    correct:1
  },
  {
    q:"Según el modelo SCARF, un líder que otorga reconocimiento público al desempeño de un colaborador está activando principalmente el dominio de:",
    options:["Certeza (Certainty)","Estatus (Status)","Relación (Relatedness)","Justicia (Fairness)"],
    correct:1
  },
  {
    q:"Un líder que comunica con claridad los criterios de evaluación y los pasos de un proceso institucional está favoreciendo el dominio de:",
    options:["Autonomía (Autonomy)","Certeza (Certainty)","Estatus (Status)","Relación (Relatedness)"],
    correct:1
  },
  {
    q:"Desde el neuroliderazgo, la percepción de trato injusto o inequitativo por parte del equipo tiende a activar en el cerebro una respuesta similar a la generada por:",
    options:["Una recompensa placentera","Una amenaza física o social","Un estado de relajación profunda","Ningún tipo de respuesta emocional"],
    correct:1
  },
  {
    q:"Un liderazgo que promueve espacios de decisión y participación del equipo docente en la construcción de acuerdos institucionales está fortaleciendo principalmente el dominio de:",
    options:["Autonomía (Autonomy)","Estatus (Status)","Certeza (Certainty)","Amenaza (Threat)"],
    correct:0
  }
];

const motivacionQuestions = [
  {
    q:"La motivación que surge del interés genuino, el disfrute o el sentido personal de una actividad, sin depender de recompensas externas, se denomina:",
    options:["Motivación extrínseca","Motivación intrínseca","Motivación situacional","Motivación normativa"],
    correct:1
  },
  {
    q:"La autorregulación, desde la perspectiva neuroeducativa, se refiere principalmente a la capacidad de:",
    options:["Reaccionar de forma automática ante cualquier estímulo emocional","Monitorear y ajustar los propios pensamientos, emociones y conductas en función de una meta","Suprimir por completo toda expresión emocional","Depender exclusivamente de la supervisión externa para actuar"],
    correct:1
  },
  {
    q:"La corteza prefrontal cumple un papel central en la autorregulación porque permite principalmente:",
    options:["La inhibición de impulsos y la planificación de la conducta orientada a metas","El almacenamiento permanente de recuerdos sensoriales","La regulación exclusiva del ritmo cardíaco","El procesamiento visual básico"],
    correct:0
  },
  {
    q:"Un colaborador con alta capacidad de autorregulación emocional, ante una situación de estrés institucional, tenderá a:",
    options:["Reaccionar de manera impulsiva sin evaluar consecuencias","Evitar por completo la situación sin buscar soluciones","Reconocer su estado emocional y ajustar su respuesta de forma adaptativa","Delegar siempre la gestión emocional en otra persona"],
    correct:2
  },
  {
    q:"Desde el punto de vista neuroeducativo, ofrecer metas claras, retroalimentación oportuna y cierto grado de autonomía en las tareas favorece principalmente:",
    options:["La disminución total de la motivación intrínseca","El fortalecimiento de la motivación intrínseca y la autorregulación","La dependencia exclusiva de la motivación extrínseca","La eliminación de la necesidad de retroalimentación futura"],
    correct:1
  }
];

function shuffleArray(arr){
  const a = arr.slice();
  for(let i = a.length - 1; i > 0; i--){
    const j = Math.floor(Math.random() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}

// Reordena las opciones de cada pregunta y recalcula el índice de la respuesta correcta.
// Se ejecuta una vez por carga de página, así cada intento presenta un orden distinto.
function shuffleQuestionOptions(list){
  return list.map(item=>{
    const indices = shuffleArray(item.options.map((_, i)=>i));
    const newOptions = indices.map(i=>item.options[i]);
    const newCorrect = indices.indexOf(item.correct);
    return { q:item.q, options:newOptions, correct:newCorrect };
  });
}

const liderazgoQuestionsShuffled = shuffleQuestionOptions(liderazgoQuestions);
const motivacionQuestionsShuffled = shuffleQuestionOptions(motivacionQuestions);

function buildQuestions(list, container, prefix){
  list.forEach((item, idx)=>{
    const div = document.createElement('div');
    div.className = 'question';
    div.id = prefix + '-' + idx;
    let optsHtml = '';
    item.options.forEach((opt, oidx)=>{
      optsHtml += `<label><input type="radio" name="${prefix}-${idx}" value="${oidx}"> ${opt}</label>`;
    });
    div.innerHTML = `<p class="qtext"><span class="qnum">${idx+1}.</span>${item.q}</p>
      <div class="options">${optsHtml}</div>
      <div class="feedback" id="fb-${prefix}-${idx}"></div>`;
    container.appendChild(div);
  });
}

buildQuestions(liderazgoQuestionsShuffled, document.getElementById('liderazgoBlock'), 'liderazgo');
buildQuestions(motivacionQuestionsShuffled, document.getElementById('motivacionBlock'), 'motivacion');

document.getElementById('submitBtn').addEventListener('click', function(){
  let allAnswered = true;
  let liderazgoCorrect = 0;
  let motivacionCorrect = 0;

  liderazgoQuestionsShuffled.forEach((item, idx)=>{
    const selected = document.querySelector(`input[name="liderazgo-${idx}"]:checked`);
    const qDiv = document.getElementById('liderazgo-' + idx);
    const fb = document.getElementById('fb-liderazgo-' + idx);
    if(!selected){
      allAnswered = false;
      qDiv.classList.remove('correct','incorrect');
      fb.classList.remove('show');
      return;
    }
    const val = parseInt(selected.value);
    if(val === item.correct){
      liderazgoCorrect++;
      qDiv.classList.add('correct');
      qDiv.classList.remove('incorrect');
      fb.textContent = 'Correcto.';
      fb.className = 'feedback show correct';
    } else {
      qDiv.classList.add('incorrect');
      qDiv.classList.remove('correct');
      fb.textContent = 'Incorrecto. Respuesta correcta: ' + item.options[item.correct];
      fb.className = 'feedback show incorrect';
    }
  });

  motivacionQuestionsShuffled.forEach((item, idx)=>{
    const selected = document.querySelector(`input[name="motivacion-${idx}"]:checked`);
    const qDiv = document.getElementById('motivacion-' + idx);
    const fb = document.getElementById('fb-motivacion-' + idx);
    if(!selected){
      allAnswered = false;
      qDiv.classList.remove('correct','incorrect');
      fb.classList.remove('show');
      return;
    }
    const val = parseInt(selected.value);
    if(val === item.correct){
      motivacionCorrect++;
      qDiv.classList.add('correct');
      qDiv.classList.remove('incorrect');
      fb.textContent = 'Correcto.';
      fb.className = 'feedback show correct';
    } else {
      qDiv.classList.add('incorrect');
      qDiv.classList.remove('correct');
      fb.textContent = 'Incorrecto. Respuesta correcta: ' + item.options[item.correct];
      fb.className = 'feedback show incorrect';
    }
  });

  const warning = document.getElementById('missingWarning');
  if(!allAnswered){
    warning.classList.add('show');
    document.getElementById('resultBox').classList.remove('show');
    return;
  }
  warning.classList.remove('show');

  const total = liderazgoCorrect + motivacionCorrect;
  const percent = Math.round((total/10)*100);

  document.getElementById('scoreNum').textContent = total + ' / 10';
  document.getElementById('scorePercent').textContent = percent + '% de respuestas correctas';
  document.getElementById('liderazgoScore').textContent = liderazgoCorrect + '/5';
  document.getElementById('motivacionScore').textContent = motivacionCorrect + '/5';

  let message = '';
  if(percent >= 90){
    message = 'Excelente dominio de los conceptos de neuroliderazgo, motivación y autorregulación.';
  } else if(percent >= 70){
    message = 'Buen desempeño. Se recomienda reforzar los temas señalados como incorrectos.';
  } else if(percent >= 50){
    message = 'Desempeño moderado. Se sugiere repasar los conceptos fundamentales antes de continuar con el siguiente submódulo.';
  } else {
    message = 'Se recomienda revisar nuevamente los contenidos del submódulo antes de avanzar.';
  }
  document.getElementById('scoreMessage').textContent = message;

  document.getElementById('resultBox').classList.add('show');
  document.getElementById('resultBox').scrollIntoView({behavior:'smooth'});
});
</script>
</body>
</html>