<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Evaluación: Comunidad, Evidencia y Mejora Institucional</title>
<style>
  :root{
    --primary:#1f4e5f;
    --accent:#e07a3f;
    --bg:#f6f3ee;
    --card:#ffffff;
    --text:#2b2b2b;
    --correct:#2e7d32;
    --incorrect:#c62828;
    --border:#ddd6c9;
  }
  *{box-sizing:border-box;}
  body{
    margin:0;
    font-family:'Georgia', 'Times New Roman', serif;
    background:var(--bg);
    color:var(--text);
    padding:24px 12px 60px;
  }
  .container{max-width:820px;margin:0 auto;}
  header{
    background:var(--primary);
    color:#fff;
    padding:28px 24px;
    border-radius:10px;
    margin-bottom:24px;
  }
  header h1{margin:0 0 6px;font-size:1.5rem;}
  header p{margin:0;font-size:0.95rem;opacity:0.9;}
  header .intro{
    margin-top:12px;
    font-size:0.88rem;
    font-family:Arial, sans-serif;
    line-height:1.5;
    opacity:0.95;
    border-top:1px solid rgba(255,255,255,0.25);
    padding-top:12px;
  }
  .badge{
    display:inline-block;
    background:var(--accent);
    color:#fff;
    padding:3px 10px;
    border-radius:20px;
    font-size:0.75rem;
    margin-top:10px;
    font-family:Arial, sans-serif;
    letter-spacing:0.5px;
  }
  .section-label{
    font-family:Arial, sans-serif;
    text-transform:uppercase;
    letter-spacing:1px;
    font-size:0.75rem;
    color:var(--accent);
    font-weight:bold;
    margin:30px 0 6px;
    border-bottom:2px solid var(--accent);
    padding-bottom:4px;
  }
  .question{
    background:var(--card);
    border:1px solid var(--border);
    border-radius:8px;
    padding:18px 20px;
    margin-bottom:14px;
  }
  .question p.qtext{
    font-weight:bold;
    margin:0 0 12px;
    font-size:1.02rem;
  }
  .qnum{color:var(--primary);margin-right:6px;}
  .options label{
    display:block;
    padding:8px 10px;
    border-radius:6px;
    margin-bottom:6px;
    cursor:pointer;
    font-family:Arial, sans-serif;
    font-size:0.95rem;
    transition:background 0.15s;
  }
  .options label:hover{background:#f0ece2;}
  .options input{margin-right:8px;}
  .question.correct{border-left:5px solid var(--correct);}
  .question.incorrect{border-left:5px solid var(--incorrect);}
  .feedback{
    display:none;
    margin-top:10px;
    padding:8px 10px;
    border-radius:6px;
    font-family:Arial, sans-serif;
    font-size:0.88rem;
  }
  .feedback.show{display:block;}
  .feedback.correct{background:#e8f5e9;color:var(--correct);}
  .feedback.incorrect{background:#fdecea;color:var(--incorrect);}
  #submitBtn{
    display:block;
    width:100%;
    padding:14px;
    background:var(--primary);
    color:#fff;
    border:none;
    border-radius:8px;
    font-family:Arial, sans-serif;
    font-size:1.05rem;
    font-weight:bold;
    cursor:pointer;
    margin-top:24px;
  }
  #submitBtn:hover{background:#173d4a;}
  #resultBox{
    display:none;
    margin-top:24px;
    background:var(--card);
    border:2px solid var(--primary);
    border-radius:10px;
    padding:24px;
    text-align:center;
    font-family:Arial, sans-serif;
  }
  #resultBox.show{display:block;}
  #resultBox h2{color:var(--primary);margin-top:0;}
  .scoreNum{font-size:2.6rem;font-weight:bold;color:var(--accent);}
  .breakdown{
    display:flex;
    justify-content:center;
    gap:30px;
    margin-top:16px;
    flex-wrap:wrap;
  }
  .breakdown div{font-size:0.9rem;}
  .breakdown strong{display:block;font-size:1.3rem;color:var(--primary);}
  .missing-warning{
    color:var(--incorrect);
    font-family:Arial, sans-serif;
    font-size:0.9rem;
    text-align:center;
    margin-top:10px;
    display:none;
  }
  .missing-warning.show{display:block;}
</style>
</head>
<body>
<div class="container">
  <header>
    <h1>Evaluación de Submódulo</h1>
    <p>Comunidad, Evidencia y Mejora Institucional</p>
    <span class="badge">10 preguntas · Selección única</span>
    <p class="intro">Esta evaluación integra lo aprendido mediante una reflexión aplicada sobre buenas prácticas, evidencia institucional y mejora continua en la gestión educativa.</p>
  </header>

  <form id="quizForm">
    <div class="section-label">Bloque I · Comunidad (5 preguntas)</div>
    <div id="comunidadBlock"></div>

    <div class="section-label">Bloque II · Evidencia y Mejora Institucional (5 preguntas)</div>
    <div id="evidenciaBlock"></div>

    <p class="missing-warning" id="missingWarning">Por favor responde todas las preguntas antes de enviar.</p>
    <button type="button" id="submitBtn">Enviar y calcular resultado</button>
  </form>

  <div id="resultBox">
    <h2>Resultado de la evaluación</h2>
    <div class="scoreNum" id="scoreNum">0 / 10</div>
    <p id="scorePercent"></p>
    <div class="breakdown">
      <div><strong id="comunidadScore">0/5</strong>Comunidad</div>
      <div><strong id="evidenciaScore">0/5</strong>Evidencia y Mejora Institucional</div>
    </div>
    <p id="scoreMessage" style="margin-top:16px;font-style:italic;"></p>
  </div>
</div>

<script>
const comunidadQuestions = [
  {
    q:"Una comunidad profesional de aprendizaje al interior de un centro educativo se caracteriza principalmente por:",
    options:["Docentes que trabajan de forma aislada sin compartir prácticas","Un grupo de educadores que colabora de manera sostenida, comparte evidencia y reflexiona sobre su práctica para mejorar el aprendizaje","Reuniones administrativas centradas exclusivamente en trámites","La ausencia de objetivos comunes entre el personal docente"],
    correct:1
  },
  {
    q:"La participación activa de familias y comunidad en la vida institucional, desde una gestión con enfoque neuroeducativo, contribuye principalmente a:",
    options:["Fortalecer redes de apoyo, confianza y sentido de pertenencia que favorecen el bienestar y el aprendizaje","Aumentar la carga administrativa sin ningún beneficio pedagógico","Sustituir el rol formativo del cuerpo docente","Reducir la autonomía institucional del centro educativo"],
    correct:0
  },
  {
    q:"Un director que promueve el liderazgo distribuido, involucrando a docentes, familias y estudiantes en la toma de decisiones institucionales, está fortaleciendo principalmente:",
    options:["Un modelo de gestión centralizado y jerárquico","El sentido de comunidad y corresponsabilidad institucional","La dependencia exclusiva de la autoridad directiva","El aislamiento de los distintos actores educativos"],
    correct:1
  },
  {
    q:"Ante un caso de baja participación de las familias en actividades institucionales, una buena práctica orientada a fortalecer la comunidad educativa sería:",
    options:["Eliminar los espacios de participación familiar por considerarlos poco efectivos","Sancionar a las familias que no asisten a las convocatorias","Diversificar los canales de comunicación y crear espacios de participación significativos y accesibles","Delegar la comunicación con las familias únicamente al personal administrativo"],
    correct:2
  },
  {
    q:"La construcción de una cultura institucional colaborativa, con relaciones de confianza entre docentes, dirección, estudiantes y familias, favorece principalmente:",
    options:["Un clima socioemocional que facilita el aprendizaje y reduce la percepción de amenaza social","El aumento de la competitividad individual entre docentes","La disminución de la cohesión institucional","La eliminación de instancias de retroalimentación"],
    correct:0
  }
];

const evidenciaQuestions = [
  {
    q:"La toma de decisiones institucionales basada en evidencia implica principalmente:",
    options:["Decidir exclusivamente con base en la intuición del equipo directivo","Recopilar, analizar e interpretar datos institucionales confiables para orientar la acción y la mejora","Ignorar los resultados de los procesos de evaluación institucional","Aplicar siempre las mismas estrategias sin revisar sus resultados"],
    correct:1
  },
  {
    q:"Un ciclo de mejora continua institucional (por ejemplo, Planificar-Hacer-Verificar-Actuar) tiene como propósito principal:",
    options:["Implementar cambios de manera permanente sin revisión posterior","Generar un proceso cíclico de planificación, implementación, evaluación y ajuste de las prácticas institucionales","Evitar cualquier tipo de cambio en la gestión educativa","Centrar la mejora únicamente en aspectos de infraestructura"],
    correct:1
  },
  {
    q:"Ante resultados de una autoevaluación institucional que evidencian debilidades en la comunicación entre niveles educativos, una acción de mejora continua coherente sería:",
    options:["Ignorar los resultados por considerarlos poco relevantes","Diseñar e implementar un plan de acción específico, con seguimiento y evaluación de su impacto","Aplazar cualquier intervención hasta el siguiente ciclo lectivo sin analizar causas","Atribuir el problema únicamente a factores externos a la institución"],
    correct:1
  },
  {
    q:"La sistematización de buenas prácticas institucionales (documentarlas, analizarlas y compartirlas) tiene como principal beneficio:",
    options:["Aumentar la carga de trabajo sin ningún valor agregado","Facilitar el aprendizaje organizacional y la réplica de estrategias efectivas en otros contextos institucionales","Reducir la posibilidad de mejora futura","Sustituir la necesidad de evaluación institucional"],
    correct:1
  },
  {
    q:"Para que un plan de mejora institucional sea sostenible en el tiempo, es fundamental que:",
    options:["Dependa exclusivamente de la gestión de una sola persona","Se revise una única vez al inicio del proceso sin ajustes posteriores","Se institucionalice mediante seguimiento continuo, evidencia y participación de la comunidad educativa","Se mantenga en secreto para evitar resistencias del personal"],
    correct:2
  }
];

function shuffleArray(arr){
  const a = arr.slice();
  for(let i = a.length - 1; i > 0; i--){
    const j = Math.floor(Math.random() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}

// Reordena las opciones de cada pregunta y recalcula el índice de la respuesta correcta.
// Se ejecuta una vez por carga de página, así cada intento presenta un orden distinto.
function shuffleQuestionOptions(list){
  return list.map(item=>{
    const indices = shuffleArray(item.options.map((_, i)=>i));
    const newOptions = indices.map(i=>item.options[i]);
    const newCorrect = indices.indexOf(item.correct);
    return { q:item.q, options:newOptions, correct:newCorrect };
  });
}

const comunidadQuestionsShuffled = shuffleQuestionOptions(comunidadQuestions);
const evidenciaQuestionsShuffled = shuffleQuestionOptions(evidenciaQuestions);

function buildQuestions(list, container, prefix){
  list.forEach((item, idx)=>{
    const div = document.createElement('div');
    div.className = 'question';
    div.id = prefix + '-' + idx;
    let optsHtml = '';
    item.options.forEach((opt, oidx)=>{
      optsHtml += `<label><input type="radio" name="${prefix}-${idx}" value="${oidx}"> ${opt}</label>`;
    });
    div.innerHTML = `<p class="qtext"><span class="qnum">${idx+1}.</span>${item.q}</p>
      <div class="options">${optsHtml}</div>
      <div class="feedback" id="fb-${prefix}-${idx}"></div>`;
    container.appendChild(div);
  });
}

buildQuestions(comunidadQuestionsShuffled, document.getElementById('comunidadBlock'), 'comunidad');
buildQuestions(evidenciaQuestionsShuffled, document.getElementById('evidenciaBlock'), 'evidencia');

document.getElementById('submitBtn').addEventListener('click', function(){
  let allAnswered = true;
  let comunidadCorrect = 0;
  let evidenciaCorrect = 0;

  comunidadQuestionsShuffled.forEach((item, idx)=>{
    const selected = document.querySelector(`input[name="comunidad-${idx}"]:checked`);
    const qDiv = document.getElementById('comunidad-' + idx);
    const fb = document.getElementById('fb-comunidad-' + idx);
    if(!selected){
      allAnswered = false;
      qDiv.classList.remove('correct','incorrect');
      fb.classList.remove('show');
      return;
    }
    const val = parseInt(selected.value);
    if(val === item.correct){
      comunidadCorrect++;
      qDiv.classList.add('correct');
      qDiv.classList.remove('incorrect');
      fb.textContent = 'Correcto.';
      fb.className = 'feedback show correct';
    } else {
      qDiv.classList.add('incorrect');
      qDiv.classList.remove('correct');
      fb.textContent = 'Incorrecto. Respuesta correcta: ' + item.options[item.correct];
      fb.className = 'feedback show incorrect';
    }
  });

  evidenciaQuestionsShuffled.forEach((item, idx)=>{
    const selected = document.querySelector(`input[name="evidencia-${idx}"]:checked`);
    const qDiv = document.getElementById('evidencia-' + idx);
    const fb = document.getElementById('fb-evidencia-' + idx);
    if(!selected){
      allAnswered = false;
      qDiv.classList.remove('correct','incorrect');
      fb.classList.remove('show');
      return;
    }
    const val = parseInt(selected.value);
    if(val === item.correct){
      evidenciaCorrect++;
      qDiv.classList.add('correct');
      qDiv.classList.remove('incorrect');
      fb.textContent = 'Correcto.';
      fb.className = 'feedback show correct';
    } else {
      qDiv.classList.add('incorrect');
      qDiv.classList.remove('correct');
      fb.textContent = 'Incorrecto. Respuesta correcta: ' + item.options[item.correct];
      fb.className = 'feedback show incorrect';
    }
  });

  const warning = document.getElementById('missingWarning');
  if(!allAnswered){
    warning.classList.add('show');
    document.getElementById('resultBox').classList.remove('show');
    return;
  }
  warning.classList.remove('show');

  const total = comunidadCorrect + evidenciaCorrect;
  const percent = Math.round((total/10)*100);

  document.getElementById('scoreNum').textContent = total + ' / 10';
  document.getElementById('scorePercent').textContent = percent + '% de respuestas correctas';
  document.getElementById('comunidadScore').textContent = comunidadCorrect + '/5';
  document.getElementById('evidenciaScore').textContent = evidenciaCorrect + '/5';

  let message = '';
  if(percent >= 90){
    message = 'Excelente integración de los conceptos de comunidad, evidencia y mejora institucional continua.';
  } else if(percent >= 70){
    message = 'Buen desempeño. Se recomienda reforzar los temas señalados como incorrectos.';
  } else if(percent >= 50){
    message = 'Desempeño moderado. Se sugiere repasar los conceptos de comunidad educativa y de mejora basada en evidencia.';
  } else {
    message = 'Se recomienda revisar nuevamente los contenidos del submódulo antes de avanzar.';
  }
  document.getElementById('scoreMessage').textContent = message;

  document.getElementById('resultBox').classList.add('show');
  document.getElementById('resultBox').scrollIntoView({behavior:'smooth'});
});
</script>
</body>
</html>